<!--コメントの一覧も入力フォームと同様にパーシャルで作成します。-->
<ul>
  <% comments.each do |comment| %>
    <% if comment.id.present? %>
      <li>
        <p><%= comment.content %></p>
      </li>
    <% end %>
  <% end %>
</ul>
